# import Member from 'members.graphql'
# import Forum from 'forums.graphql'

type Message {
  id: ID!
  message: String!
  forum: Forum
  user: Member
  createdAt: String
}

type Forum {
  id: ID!
  name: String
  messages: [Message]
  members: [Member]
}
